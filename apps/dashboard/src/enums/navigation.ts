export enum Route {
  HOME = "/",
  LOGIN = "/auth/login",
  SIGNUP = "/auth/signup",
  PROFILE = "/profile",
  PORTFOLIO = "/portfolio",
  OFFERS = "/offers",
  PAYOUTS = "/payouts",
  STAT = "/stat",
  SETTINGS = "/settings",
}
