export enum Route {
  HOME = "/",
  LOGIN = "/auth/login",
  SIGNUP = "/auth/signup",
  PROFILE = "/profile",
  OFFERS = "/offers",
  SUBSCRIPTION = "/subscription",
  STAT = "/stat",
  SETTINGS = "/settings",
}
