export enum Route {
  HOME = "/my",
  LOGIN = "/auth/login",
  SIGNUP = "/auth/signup",
  PROFILE = "/my/profile",
  OFFERS = "/my/offers",
  SUBSCRIPTION = "/my/subscription",
  STAT = "/my/stat",
  SETTINGS = "/my/settings",
}
