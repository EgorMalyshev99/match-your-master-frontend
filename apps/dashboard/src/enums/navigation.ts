export enum Route {
  HOME = "/",
  LOGIN = "/login",
  SIGNUP = "/signup",
  PROFILE = "/profile",
  PORTFOLIO = "/portfolio",
  OFFERS = "/offers",
  PAYOUTS = "/payouts",
  STAT = "/stat",
  SETTINGS = "/settings",
}
